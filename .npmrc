# pnpm 11 verifies every lockfile entry against a minimum-release-age policy,
# which asks the registry for each package publish time. On a shared CI IP that
# is thousands of requests and npm answers 429. We pin dependencies by lockfile,
# so the age gate buys nothing here.
minimum-release-age=0
